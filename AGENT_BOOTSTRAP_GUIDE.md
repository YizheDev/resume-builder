版本：v2.0
日期：2026-06-03
适用范围：任何使用 AI Agent 进行软件开发的团队项目


---

零、项目冷启动：用这份文档自举 Agent 开发环境
0.1 设计意图
本指南是一份自举（Self-Bootstrapping）文档。

它的设计目标是：你拿到一个全新的空白项目，只把这份文档放进项目目录，对 AI Agent 说一句话：
"请根据 docs/AGENT_DEVELOPMENT_GUIDE.md 为零章"项目冷启动"部分的规范，为当前项目生成完整的 Agent 开发架构。请逐个文档生成，每生成一个文档后停下来让我确认。"

Agent 将自动完成以下所有工作：
1. 询问项目基本信息（名称、技术栈、团队结构）
2. 逐文件生成完整的 Agent 开发文档体系
3. 创建目录结构
4. 配置 .gitignore
   之后你就可以直接在项目中用 Agent 进行开发了。

0.2 冷启动前的准备
在启动 Agent 之前，你需要准备好以下信息（Agent 会在生成过程中逐步询问）：
□ 项目名称（中文 + 英文）
□ 项目简短描述（一句话）
□ 技术栈：
- 后端框架和语言（如 Spring Boot 3.x + Java 21）
- 前端框架（如 Vue 3 + TypeScript）
- 数据库类型（如 MySQL 8.4）
- 构建工具（如 Maven / Gradle）
- 包管理器（如 pnpm / npm）
  □ 代码仓库地址
  □ 团队结构：负责人、审批人
  □ 项目阶段（初始化 / 开发中 / 维护期）
  □ 是否需要 CI/CD（Jenkins / GitHub Actions）
  □ 是否需要容器化（Docker / K8s）

0.3 Agent 冷启动执行流程
用户将本指南放入项目目录
↓
用户对 Agent 说："根据 零章 生成 Agent 开发架构"
↓
Agent 读取 零章 → 逐个询问项目信息 → 逐文件生成
↓
┌────────────────────────────────────────────┐
│ 第一阶段：基础文档（必须生成）              │
│  ├── .gitignore                            │
│  ├── AGENTS.md                             │
│  ├── docs/CURRENT_STATE.md                 │
│  ├── docs/DEVELOPMENT_LOG.md               │
│  ├── docs/TECHNICAL_SPEC.md                │
│  ├── docs/ARCHITECTURE.md                  │
│  ├── docs/DATABASE_DESIGN.md               │
│  ├── docs/ROADMAP.md                       │
│  └── docs/DECISIONS.md                     │
├────────────────────────────────────────────┤
│ 第二阶段：AI 协作模板（强烈推荐生成）       │
│  ├── docs/ai/AGENT_TASK_TEMPLATE.md        │
│  ├── docs/ai/AGENT_REVIEW_CHECKLIST.md     │
│  ├── docs/ai/AGENT_HANDOFF_TEMPLATE.md     │
│  ├── docs/ai/AGENT_DECISION_RULES.md       │
│  ├── docs/ai/AGENT_PR_TEMPLATE.md          │
│  └── docs/ai/PROMPT_EXAMPLES.md            │
├────────────────────────────────────────────┤
│ 第三阶段：任务记录（空目录）                │
│  └── docs/tasks/                           │
└────────────────────────────────────────────┘

0.4 每个文档的详细生成规范
文档1：.gitignore
用途： 排除不应提交到 Git 的文件。
生成规则： Agent 必须根据项目技术栈自动生成对应的 .gitignore 规则，按需增删配置。
# ===== 编译产物 =====
target/
build/
dist/
out/
*.class
*.jar
*.war

# ===== 依赖 =====
node_modules/
.pnpm-store/

# ===== 环境配置（含密钥）=====
.env
.env.local
.env.*.local
deploy/.env

# ===== 操作系统 =====
.DS_Store
Thumbs.db

# ===== IDE =====
.idea/
.vscode/
*.iml
*.iws
*.ipr

# ===== 日志 =====
logs/
*.log

# ===== 测试产物 =====
test-results/
playwright-report/
*.xlsx
*.xls

# ===== 临时文件 =====
*.tmp
*.swp
*.bak
/tmp/

# ===== AI 工具 =====
.claude/
.understand-anything/

# ===== 前端 =====
.vite/
Agent 生成动作：
1. 询问项目构建工具，精准配置产物目录过滤
2. 确认项目独有需要忽略的自定义文件
3. 逐条备注规则释义
   文档2：AGENTS.md
   用途： Agent 的总入口文档，每次会话启动优先读取。
# AGENTS.md — <项目名称> AI 开发强制规则
> 适用范围：本仓库全部目录。除非子目录存在更具体的 AGENTS.md，否则本文件优先级最高。
> 项目目标：使用 AI 进行主要开发，人类进行架构把关、代码审查、上线批准和风险兜底。

## 0. 项目身份
- 公司名称：<公司名>
- 项目名称：<项目英文名>
- 项目描述：<一句话描述>
- 后端：<语言 + 框架>（如 Java 21 + Spring Boot 3.5.x）
- 前端：<框架 + 语言>（如 Vue 3 + TypeScript + Vite）
- 数据库：<数据库类型和版本>
- 根包名：<Java 根包名 / 前端模块名>
- 当前阶段：<初始化 / 开发中 / 维护期>
- 当前重点：<当前阶段的主要业务模块>

## 1. AI 工作前必须阅读的文档
每次开始开发前，AI 必须先阅读并遵守以下文档，按优先级排列：
1. `docs/TECHNICAL_SPEC.md` — 技术规范（技术栈、编码规范、日志规范），每次必读
2. `docs/ARCHITECTURE.md` — 系统架构（模块划分、依赖方向），每次必读
3. `docs/<业务模块>_SPEC.md` — 如本次任务涉及该业务模块，必读
4. `docs/DATABASE_DESIGN.md` — 如本次任务涉及数据库变更，必读
5. `docs/CURRENT_STATE.md` — 当前系统状态（已完成/未完成），每次必读
6. `docs/DEVELOPMENT_LOG.md` — 开发日志（最近变化），每次必读
7. `docs/ROADMAP.md` — 路线图，如影响长期计划时必读
8. `docs/DECISIONS.md` — 决策记录，如涉及项目决策时必读
9. `docs/ai/AGENT_DECISION_RULES.md` — Agent 决策优先级规则
10. `docs/ai/AGENT_TASK_TEMPLATE.md` — 任务记录模板，新建任务时参考
11. `docs/ai/AGENT_REVIEW_CHECKLIST.md` — 评审清单，自检或 Review 时使用
12. `docs/ai/AGENT_HANDOFF_TEMPLATE.md` — 交接模板，会话切换时使用
13. `docs/ai/AGENT_PR_TEMPLATE.md` — PR 描述模板，准备 PR 时使用

**关键原则：**
- AI 不得只依赖历史会话记忆。所有关键上下文必须从仓库文档读取。
- 若仓库文档与聊天记忆冲突，以仓库文档为准。
- 若仓库文档与当前代码事实冲突，必须先判断文档是否过期，并把结论写回任务记录与相关文档。

## 2. 编码规范优先级
本项目采用以下规范，冲突时按优先级执行：
1. 本仓库 `docs/TECHNICAL_SPEC.md` 中的项目级约束 — **最高优先级**
2. <团队约定的编码规范，如阿里巴巴 Java 手册 / Google Style Guide>
3. 多个任务输入或规则冲突时，按 `docs/ai/AGENT_DECISION_RULES.md` 中的决策优先级执行

## 3. AI 开发硬性限制（14 条）
### 代码层面
1. 不得删除业务代码、迁移脚本、文档、测试，除非任务明确要求，并且在任务记录中写明原因和影响。
2. 不得引入新框架、新中间件、新外部服务，除非先新增决策记录（ADR）并得到人工确认。
3. 不得修改数据库结构而不新增迁移脚本（如使用 Flyway 则必须新增 V<N>__xxx.sql）。
4. 不得直接修改已发布的迁移脚本，只能新增版本脚本。
5. 不得把密钥、Token、API Key、数据库密码写入代码、配置文件、测试文件、文档或提交记录。
6. 不得在业务日志中打印敏感信息，包括：API Key、Authorization Header、手机号完整值、邮箱完整值、客户地址完整值。
7. 不得让 Controller 直接访问 Mapper 或第三方 API。必须通过 Service 层。
8. 不得让业务模块直接访问其他模块的 Entity、Mapper、Repository。
9. 不得跨模块执行数据库 join。
10. 不得吞掉异常。捕获异常后必须记录上下文，并转为业务异常或继续抛出。

### 测试层面
11. 不得为了通过测试而删除断言或降低测试价值。
12. 不得在没有测试或验证说明的情况下标记任务完成。

### E2E 层面
13. 不得在没有证据的情况下宣称 E2E 通过。E2E 必须记录：测试环境、覆盖场景、执行结果、证据、未覆盖项。
14. 不得在长周期任务中只依赖聊天上下文继续工作。必须通过任务记录、开发日志、当前状态和 handoff 文档恢复现场。

## 4. 每次任务必须产出的文档
每次开发必须更新以下至少三类文档：
1. `docs/tasks/YYYY-MM-DD-<任务名>.md` — **任务记录，必须有**
2. `docs/DEVELOPMENT_LOG.md` — **开发日志，必须追加**
3. `docs/CURRENT_STATE.md` — 如果系统状态、模块进度、接口、表结构有变化，必须更新
4. `docs/ARCHITECTURE.md` — 如果模块边界、依赖方向、架构策略改变，必须更新
5. `docs/DATABASE_DESIGN.md` — 如果表结构、索引、字段语义改变，必须更新
6. `docs/ROADMAP.md` — 如果本次任务影响长期路线、优先级或治理阶段，必须更新
7. `docs/DECISIONS.md` — 如果本次任务形成需要长期保留的关键执行决策，必须更新

**会话断开、模型切换或人员更换时，新的开发者必须能够只靠仓库文档继续工作。**

## 5. 任务执行标准流程（11 步）
AI 必须按以下流程工作：
1. 读取上下文文档（本文件第 1 节列出的文档）
2. 明确任务范围和非目标（写清楚"做什么"和"不做什么"）
3. 新建或更新任务记录（docs/tasks/YYYY-MM-DD-<任务名>.md）
4. 列出拟修改文件清单
5. 小步修改代码（每次只改一个关注点）
6. 同步修改测试
7. 同步修改文档
8. 运行可用验证命令（编译、测试、lint）
9. 记录验证结果和未验证项
10. 更新 DEVELOPMENT_LOG.md 和 CURRENT_STATE.md
11. 输出交付摘要、风险、后续建议

## 6. 任务完成定义
一个任务只有满足以下条件才可视为完成：
1. 代码符合规范
2. 编译通过
3. 单元测试通过，或明确记录无法执行的原因
4. 必要接口有 API 文档说明
5. 必要数据库变更有迁移脚本
6. 必要文档已更新
7. 开发日志已追加
8. 没有未说明的 TODO、临时代码、硬编码密钥
9. PR 描述包含变更范围、验证方式、风险和回滚方案
10. 安全检查已完成
11. 若任务包含 E2E，则已记录环境、用例、结果、证据和未覆盖项
12. 若任务为长周期任务，则已保证下一位开发者可只依赖仓库文档继续工作
13. 若任务进入 Review/合并阶段，则已优先按 PR 模板准备 PR 描述

## 7. 推荐 AI 分工
- AI Developer：实现代码、补测试、更新任务文档
- AI Reviewer：审查架构边界、编码规范、测试覆盖、数据库变更、安全风险
- Human Approver：审批技术选型、依赖引入、数据库发布、生产上线、敏感操作

AI 可以完成大部分编码工作，但最终上线必须由人类确认。
Agent 生成动作：
1. 替换项目、公司、技术栈占位信息
2. 填充项目约定编码规范
3. 确认14条限制规则是否需要增减，生成后人工审核
   文档3：docs/CURRENT_STATE.md
# 当前系统状态
最后更新：YYYY-MM-DD
维护人：<负责人姓名>

---

## 1. 项目基本信息
- 公司：<公司名>
- 项目：<项目名>
- 项目描述：<一句话描述>
- 当前阶段：初始化（项目刚创建，尚未开始开发）
- 当前架构：<架构模式，如模块化单体>
- 当前重点：<当前阶段的主要目标>

## 2. 当前技术选型
| 组件 | 版本 | 用途 |
|------|------|------|
| <后端语言> | <版本> | 运行环境 |
| <后端框架> | <版本> | 应用框架 |
| <数据库> | <版本> | 数据存储 |
| <前端框架> | <版本> | 管理后台 |
| <构建工具> | <版本> | 编译构建 |
| <其他关键依赖> | <版本> | <用途> |

## 3. 当前模块规划
```text
<模块1>    <职责说明>
<模块2>    <职责说明>

4. 当前已完成
- 项目初始化，目录结构已创建
- AGENTS.md 和开发文档体系已建立
- .gitignore 已配置
5. 当前未完成
- <列出待开发的主要功能>
6. 当前数据库版本
无（初始化阶段，尚未创建数据库迁移脚本）

7. 当前接口列表
无（初始化阶段，尚未创建任何 API 接口）

8. 下一步建议
优先级从高到低：
1. 搭建项目骨架（Maven/Gradle 多模块、前端工程脚手架）
2. 创建数据库基线迁移脚本
3. 实现第一个业务模块的最小闭环
9. 当前风险
1. 初期架构决策影响后续扩展性，需要人类审批确认
2. 技术选型确定后难以更改，需要充分评估
**Agent 生成动作：**
1. 逐项确认技术栈版本、模块规划、项目基础信息
2. 备注文档更新规则：每次任务结束同步刷新状态，生成后人工确认

#### 文档4：`docs/DEVELOPMENT_LOG.md`
```markdown
# 开发日志
最后更新：YYYY-MM-DD

---

## <YYYY-MM-DD>：项目初始化
- 项目创建：<项目名>
- 技术选型确定：<列出技术栈>
- Agent 开发文档体系建立：AGENTS.md、CURRENT_STATE.md、本文件等
- .gitignore 配置完成

---
Agent 生成动作：
1. 填充当前日期与项目信息
2. 说明规则：任务结束末尾追加日志记录，无需人工单次确认
文档5：docs/TECHNICAL_SPEC.md
# <项目名称> 技术开发规范
版本：v1.0
日期：YYYY-MM-DD

---

## 1. 总体原则
1. 所有数据库变更必须通过迁移工具（如 Flyway / Liquibase）。
2. 禁止手工修改生产数据库结构。
3. 禁止修改已经发布的迁移脚本，只能新增脚本。
4. 表名、字段名统一使用小写下划线。
5. 表名按模块前缀隔离（如 track_、sys_、order_）。
6. 不使用物理外键，使用逻辑外键和索引。
7. 核心表必须包含审计字段：created_at、updated_at。
8. 外部系统原始报文必须保存（用于审计和排障）。
9. 删除优先软删除（deleted TINYINT DEFAULT 0）。

## 2. 编码规范
### 2.1 包结构规范
<根据实际技术栈填写>

### 2.2 命名规范
| 类型 | 命名规则 | 示例 |
|------|---------|------|
| Entity | <规则> | <示例> |
| Service | <规则> | <示例> |
| Controller | <规则> | <示例> |
| Mapper | <规则> | <示例> |

### 2.3 异常处理规范
- 不得吞掉异常，必须记录上下文并转为业务异常
- 业务异常统一使用 BizException
- 全局异常处理器统一转换为标准 API 响应

## 3. 日志规范
- 使用 SLF4J / winston / 其他日志框架
- 格式：`事件=<事件名> <关键字段1>=<值1> <关键字段2>=<值2>`
- 敏感字段必须脱敏

## 4. API 设计规范
- 统一响应格式：{ code, message, data, traceId, timestamp }
- 分页协议：{ records, total, pageNo, pageSize, totalPages }
- Long 类型 ID 必须转为 String 返回前端

## 5. 数据库设计规范
- 主键统一 BIGINT，由应用层生成
- 状态字段使用 VARCHAR，禁止数字魔法值
- 模糊查询字段需要谨慎设计，避免大表全表扫描

## 6. 安全规范
- API Key、密码只能通过环境变量注入
- 日志不输出敏感信息
- 前端不保存 API Key
Agent 生成动作：
1. 匹配项目技术栈填充语言相关编码规则
2. 确认团队已有规范参考，生成后人工审核
文档6：docs/ARCHITECTURE.md
# <项目名称> 系统架构
版本：v1.0
日期：YYYY-MM-DD

---

## 1. 架构概述
<一段话描述整体架构设计理念>

## 2. 架构图
<ASCII 架构图，描述模块、外部服务、数据库的关系>

## 3. 模块职责
| 模块 | 职责 | 依赖 |
|------|------|------|
| <模块名> | <职责> | <依赖的模块> |

## 4. 模块间通信规则
- 模块间只能通过 Service 接口通信
- 禁止跨模块直接访问数据库
- 禁止跨模块注入 Mapper

## 5. 外部服务集成
| 服务 | 用途 | 接口 |
|------|------|------|
| <外部服务名> | <用途> | <API 地址> |

## 6. 数据流
<描述核心业务流程的数据流向>
Agent 生成动作：
1. 绘制ASCII架构图，标注依赖方向
2. 完成后人工核对确认架构合理性
文档7：docs/DATABASE_DESIGN.md
# <项目名称> 数据库设计规范
版本：v1.0
日期：YYYY-MM-DD

## 1. 总体原则
<列出数据库设计的基本原则>

## 2. 表名前缀规范
| 前缀 | 模块 | 示例 |
|------|------|------|

## 3. 通用字段规范
所有核心表必须包含的审计字段定义。

## 4. 索引规范
索引命名规则和创建原则。

## 5. 迁移工具规范
<Flyway / Liquibase 等迁移工具的使用规范>

## 6. 当前表清单
（初始化阶段为空，后续每次新增表时更新）

文档8：docs/ROADMAP.md
# <项目名称> 路线图
最后更新：YYYY-MM-DD

## 1. 当前阶段
初始化阶段：搭建项目骨架、建立开发规范、创建 Agent 文档体系

## 2. 近期计划（1-2 周）
- [ ] 创建项目脚手架
- [ ] 数据库基线迁移
- [ ] 第一个业务模块 MVP

## 3. 中期计划（1-3 月）
<待规划>

## 4. 长期愿景
<待规划>

## 5. 不在路线图内的事项
<明确列出当前不做的方向>

文档9：docs/DECISIONS.md
# <项目名称> 关键决策记录
最后更新：YYYY-MM-DD

## 1. 使用说明
本文件记录不需要单独立 ADR、但又应长期保留的项目级执行决策。

## 2. 当前有效决策
（初始化阶段为空，后续每次关键决策后追加）

## 3. 决策模板
```markdown
### D-YYYY-MM-DD-<序号>：<决策标题>
结论：<一句话结论>
背景：<为什么需要做这个决策>
备选方案：<列出考虑过的方案及取舍原因>
影响：<这个决策对项目的具体影响>

#### 文档10：`docs/ai/AGENT_TASK_TEMPLATE.md`
```markdown
# 任务记录：<任务编号> <任务名称>
日期：YYYY-MM-DD
任务编号：<唯一编号>
开发者：AI Developer
审查者：AI Reviewer / Human Reviewer
状态：Draft / In Progress / Completed

## 1. 用户问题摘要
<2-3 句话描述用户遇到的问题或需求>

## 2. 本次任务范围
<明确列出要完成的事项>

## 3. 非目标
<明确列出不做什么，防止范围蔓延>

## 4. 当前方案与实施策略
<技术方案、为什么这样做、替代方案被否决的原因>

## 5. 预计修改文件
| 文件 | 操作 | 说明 |
|------|------|------|

## 6. 验证计划
<单元测试 / HTTP smoke / E2E / 手动验证>

## 7. 实现结果
<任务完成后的实际结果>

## 8. 验证结果
| 验证项 | 结果 | 说明 |
|--------|------|------|

## 9. 风险、未完成项与后续建议

文档11：docs/ai/AGENT_REVIEW_CHECKLIST.md
# Agent 代码评审检查清单

## 架构与设计
- [ ] 是否违反模块边界（跨模块直接访问）
- [ ] 是否引入不必要的抽象或中间件
- [ ] 是否有过度设计（3 行代码能解决但设计了 5 个类）

## 编码规范
- [ ] 命名是否规范（类名、方法名、变量名）
- [ ] 包结构是否符合项目规范
- [ ] 日志格式是否正确

## 数据库
- [ ] 是否有对应的迁移脚本
- [ ] 索引是否合理
- [ ] 是否修改了已发布的迁移脚本

## 安全
- [ ] 是否暴露密钥
- [ ] 日志是否脱敏
- [ ] 前端是否直接调用第三方 API

## 测试
- [ ] 新增功能是否有测试覆盖
- [ ] 测试断言是否合理

## 文档
- [ ] CURRENT_STATE.md 是否更新
- [ ] DEVELOPMENT_LOG.md 是否追加

文档12：docs/ai/AGENT_HANDOFF_TEMPLATE.md
# Handoff：<任务名称>

## 1. 当前进度
- ✅ 已完成：
- 🔄 进行中：
- ⏳ 未开始：

## 2. 工作区状态
- 当前分支：
- 已修改未提交的文件：
- 当前运行的服务：

## 3. 阻塞项
- [ ] 需要人工确认：

## 4. 下一步操作指南
下一位 Agent 请按以下步骤继续：
1. 读取 CURRENT_STATE.md 和 DEVELOPMENT_LOG.md
2. 检查 git status
3. 执行验证命令确认当前状态
4. 从任务记录第 X 步继续

文档13：docs/ai/AGENT_DECISION_RULES.md
# Agent 决策优先级规则
当多个规范/文档/指令冲突时，按以下优先级执行：

1. AGENTS.md 和 TECHNICAL_SPEC.md 中的项目级约束 — 最高优先级
2. 本文件中的 Agent 决策规则
3. 团队编码规范（如阿里巴巴 Java 手册）
4. 用户最新明确指令
5. 通用最佳实践

## 常见冲突场景处理
<列举常见冲突及处理方式>

文档14：docs/ai/AGENT_PR_TEMPLATE.md
# PR：<PR 标题>

## 变更范围
<简述这个 PR 做了什么>

## 验证方式
- [ ] 编译通过：<命令和结果>
- [ ] 单元测试：<测试数量和结果>
- [ ] E2E 测试：<测试环境、用例数、结果>

## 风险
<可能的风险和回滚方案>

## 文档更新
- [ ] CURRENT_STATE.md
- [ ] DEVELOPMENT_LOG.md
- [ ] 其他相关文档

文档15：docs/ai/PROMPT_EXAMPLES.md
# Agent 提示词编写示例

## 示例 1：新增功能
<展示如何给 Agent 编写清晰的开发任务提示词>

## 示例 2：Bug 修复
<展示如何给 Agent 编写 Bug 修复提示词>

## 示例 3：代码审查
<展示如何给 Agent 编写审查提示词>

## 编写原则
1. 写清楚目标，而不是步骤
2. 明确非目标
3. 提供上下文引用
4. 要求验证证据

0.5 冷启动后的验证
Agent 完成所有文档生成后，必须执行以下验证：
#1. 检查基础文件存在
ls AGENTS.md docs/CURRENT_STATE.md docs/DEVELOPMENT_LOG.md docs/TECHNICAL_SPEC.md

#2. 校验.gitignore规则
touch test.env
git status
rm test.env

#3. 初始化提交
git add -A && git commit -m "chore: 初始化 Agent 开发文档体系"
收尾话术： Agent 开发环境已就绪。你现在可以说'请帮我完成 XXX 功能'，我会按照文档规范进行开发。

0.6 冷启动提示词模板
请根据 docs/AGENT_DEVELOPMENT_GUIDE.md 第零章"项目冷启动"的规范，
为当前项目生成完整的 Agent 开发架构。

请按以下顺序执行：

第一阶段：先询问我项目基本信息（项目名、技术栈、团队结构等），
然后逐个生成基础文档。每生成一个文件后停下来让我确认。

第二阶段：生成 AI 协作模板文件。

第三阶段：创建 docs/tasks/ 空目录。

请严格按照指南中每个文档的模板生成，不要省略任何章节。


---

一、核心理念
注：第零章是本文档最重要的新增内容。如果你已经完成了项目初始化，可以直接跳到第一章开始阅读 Agent 协作规范。如果你是一个新项目，请严格按照第零章执行冷启动流程。
（后续章节按需补充内容）
