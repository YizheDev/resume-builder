版本：v2.0
日期：2026-06-03
适用范围：任何使用 AI Agent 进行软件开发的团队项目


---

文档导航：
- 如果你是一个全新项目，还没有任何 Agent 开发文档，请先阅读 AGENT_BOOTSTRAP_GUIDE.md（项目冷启动指南）
- 如果你已经完成了项目初始化，请继续阅读本文档


---

一、核心理念
1.1 为什么需要 Agent 开发规范
AI Agent（如 Claude Code、Cursor、Copilot）与传统开发工具的本质区别在于：它们不是"辅助工具"，而是"主要开发者"。没有规范约束的 Agent 开发会导致：
- 上下文丢失：会话切换后，新的 Agent 无法从零开始继续工作
- 决策漂移：不同 Agent 对同一问题做出不同判断，架构逐渐腐烂
- 文档断层：代码变化后文档未更新，后续开发成本指数级上升
- 安全风险：Agent 为了方便，可能把密钥写进代码或日志
- 过度设计：Agent 倾向引入复杂的抽象和中间件，远超实际需求
  本指南的核心目标：让 AI Agent 成为可管理、可预测、可交接的团队成员。

1.2 四条黄金法则
1. 仓库文档是 Agent 的唯一长期记忆——不是聊天记录
2. 每次任务至少留下一条可追溯的记录
3. 新 Agent 必须能只靠仓库文档继续工作
4. 人类始终拥有最终决策权——Agent 负责执行，人类负责把关

1.3 三层角色模型
┌─────────────────────────────────────────────────┐
│                  Human Approver                   │
│  审批：技术选型 · 依赖引入 · 数据库发布 · 上线   │
├─────────────────────────────────────────────────┤
│                  AI Reviewer                      │
│  审查：架构边界 · 编码规范 · 测试覆盖 · 安全     │
├─────────────────────────────────────────────────┤
│                  AI Developer                     │
│  执行：编码 · 测试 · 文档 · 验证                 │
└─────────────────────────────────────────────────┘

角色
职责
权限边界
AI Developer
实现代码、补测试、更新文档、执行验证
读写代码和文档，执行本地命令
AI Reviewer
审查架构、编码规范、测试覆盖、数据库变更、安全风险
只读所有文件
Human Approver
审批技术选型、依赖引入、数据库发布、生产上线、敏感操作
最终决策，不可委托

关键原则：AI 可以完成大部分编码工作，但最终上线必须由人类确认。

二、项目应该具备的文档体系
2.1 最小文档结构
项目根目录/
├── AGENTS.md                         # Agent 入口文档（每次必读）
├── docs/
│   ├── CURRENT_STATE.md              # 当前系统状态（实时更新）
│   ├── DEVELOPMENT_LOG.md            # 开发日志（每次任务追加）
│   ├── TECHNICAL_SPEC.md             # 技术规范（技术栈、编码规范、架构）
│   ├── DATABASE_DESIGN.md            # 数据库设计规范
│   ├── ARCHITECTURE.md               # 系统架构文档
│   ├── ROADMAP.md                    # 路线图与优先级
│   ├── DECISIONS.md                  # 关键决策记录
│   ├── AGENT_BOOTSTRAP_GUIDE.md      # 项目冷启动指南
│   ├── AGENT_DEVELOPMENT_GUIDE.md    # 本文档
│   ├── ai/                           # AI 协作模板
│   │   ├── AGENT_TASK_TEMPLATE.md
│   │   ├── AGENT_REVIEW_CHECKLIST.md
│   │   ├── AGENT_HANDOFF_TEMPLATE.md
│   │   ├── AGENT_DECISION_RULES.md
│   │   └── AGENT_PR_TEMPLATE.md
│   └── tasks/                        # 任务记录（按日期）
│       └── YYYY-MM-DD-<任务名>.md
└── .gitignore

如果你的项目还没有这些文档，请参阅 AGENT_BOOTSTRAP_GUIDE.md，它会指导 Agent 逐步生成以上所有文件。

2.2 文档职责与更新频率
文档
职责
更新频率
读者
AGENTS.md
Agent 总入口，定义硬性限制
很少（项目初期确定）
Agent 首次启动
CURRENT_STATE.md
系统快照
每次任务后
Agent 每次启动
DEVELOPMENT_LOG.md
开发流水账
每次任务后追加
Agent 恢复上下文
TECHNICAL_SPEC.md
技术规范
技术栈或规范变化时
Agent 编码时参考
ARCHITECTURE.md
架构说明
模块边界变化时
架构相关任务
DATABASE_DESIGN.md
数据库规范
每次数据库变更后
数据库相关任务
ROADMAP.md
路线图
优先级变化时
长期规划
DECISIONS.md
决策记录
每次关键决策后
决策参考
tasks/YYYY-MM-DD-*.md
单次任务记录
每次任务
回溯历史

三、任务生命周期管理
3.1 任务启动：8 步上下文恢复
Agent 接到新任务时，按以下顺序执行：
第 1 步：读取 AGENTS.md → 确认项目身份、技术栈、限制条件
第 2 步：读取 CURRENT_STATE.md → 了解当前状态、已完成功能、已知风险
第 3 步：读取 DEVELOPMENT_LOG.md → 了解最近开发活动、历史坑点
第 4 步：读取相关业务规范文档 → 理解业务逻辑、接口约束
第 5 步：读取已有任务记录（如存在）→ 了解前序决策和遗留问题
第 6 步：向人类确认任务范围 → "本次目标是 X，不做 Y。是否正确？"
第 7 步：创建任务记录 → docs/tasks/YYYY-MM-DD-<任务名>.md
第 8 步：开始编码

3.2 任务执行原则
1. 每次只修改一个关注点
2. 每次修改后立即运行验证
3. 代码和测试同步修改
4. 遇到不确定的情况，停下来向人类确认
5. 每完成一个模块，更新任务记录中的进度

3.3 任务完成标准
一个任务只有满足以下全部条件才可视为完成：
[x] 代码符合项目编码规范
[x] 编译命令通过
[x] 单元测试通过（或明确记录无法执行的原因）
[x] 如有接口变更，API 文档已更新
[x] 如有数据库变更，迁移脚本已新增
[x] CURRENT_STATE.md 已更新
[x] DEVELOPMENT_LOG.md 已追加
[x] 相关设计文档已同步
[x] 任务记录已写入完整的实现结果和验证结果
[x] 安全检查已完成（无密钥泄露、无敏感日志）
[x] 如含 E2E：已记录环境、用例、结果、证据、未覆盖项
[x] 如为长周期任务：已产出交接文档
3.4 任务记录格式
# 任务记录：<编号> <名称>
日期：YYYY-MM-DD
任务编号：<唯一编号>
开发者：AI Developer
状态：In Progress / Completed

## 1. 用户问题摘要
## 2. 本次任务范围
## 3. 非目标（明确不做什么）
## 4. 当前方案与实施策略
## 5. 修改文件清单
## 6. 验证计划
## 7. 实现结果
## 8. 验证结果
## 9. 风险、未完成项与后续建议

四、会话交接机制
4.1 触发条件
场景
是否必须交接
会话即将结束，任务未完成
✅ 必须
模型切换
✅ 必须
等待人工提供凭证/审批
✅ 必须
工作区存在已修改但未验证的文件
✅ 必须
任务需要多天多阶段完成
✅ 必须

4.2 交接文档模板
# Handoff：<任务名称>

## 1. 当前进度
- ✅ 已完成：
- 🔄 进行中：
- ⏳ 未开始：

## 2. 工作区状态
- 当前分支：
- 已修改未提交的文件：
- 当前运行的服务：

## 3. 阻塞项
- [ ] 需要人工确认的事项：

## 4. 下一步操作指南（下一位 Agent 直接执行）
1. 读取 CURRENT_STATE.md 和 DEVELOPMENT_LOG.md
2. 检查 git status
3. 执行验证命令确认当前状态
4. 从任务记录第 X 步继续

## 5. 已知坑点

## 6. 关键命令

4.3 交接检查清单
[x] 任务记录已更新到最新状态
[x] DEVELOPMENT_LOG.md 已追加
[x] 未提交的代码已通过编译
[x] 交接文档中包含"下一步操作指南"
[x] 所有阻塞项已明确标注
五、决策记录机制
5.1 什么需要记录
判断标准：如果这个选择在 3 个月后无法从代码中直接看出原因，就需要记录。

需要记录
不需要记录
选择方案 A 而不是 B 的原因
变量命名、方法拆分
引入或拒绝某个框架/中间件
使用已有工具类
架构取舍（单体 vs 微服务）
Bug 修复方案
安全策略（密钥管理方式）
测试用例编写
数据库设计决策
代码格式化

5.2 决策记录模板
# 决策记录：<决策标题>
日期：YYYY-MM-DD
编号：D-YYYY-MM-DD-<序号>

## 背景
<描述场景和约束条件>

## 决策内容
<一句话陈述结论>

## 备选方案
| 方案 | 优点 | 缺点 | 结论 |
|------|------|------|------|
| A    | ...  | ...  | ✅   |
| B    | ...  | ...  | ❌   |

## 影响
<对代码、文档、团队的具体影响>

六、安全检查体系
6.1 每次任务完成前检查
# 密钥关键词筛查
git diff | grep -iE "api.?key|secret|password|token|sk_|asat_"
# 有输出 → 立即停止，通知人类
[x] application*.yml 无真实密码
[x] .env 文件在 .gitignore 中
[x] 无 .xlsx/.csv 测试数据被提交
[x] 日志中无 API Key 打印
[x] 前端无硬编码 API Key
[x] 文档示例使用占位符而非真实值
6.2 敏感信息分级
级别
信息类型
存储
日志
🔴
API Key、数据库密码
环境变量 / K8s Secret
绝对不可记录
🟡
手机号、邮箱、地址
数据库加密
脱敏后可记录
🟢
架构图、技术方案
仓库文档
正常记录

6.3 密钥泄露应急
1. 立即通知项目负责人
2. 在服务商后台轮换密钥
3. git log 检查泄露范围
4. 清理 Git 历史
5. 更新所有环境变量
6. 写事故报告

七、AI Agent 十大常见错误
1. 依赖聊天记忆，忽视文档
   防范：强制执行"每次任务写入文档"纪律。
2. 过度设计
   防范：问"当前需求真的需要这个吗？"，不需要就省略。
3. 修改已发布的迁移脚本
   防范：只能新增 V5、V6，永远不修改已发布的。
4. 假装测试通过
   防范：必须有实际执行结果，E2E 五项齐全。
5. 密钥写进代码
   防范：环境变量注入，提交前 git diff 检查。
6. 吞掉异常
   防范：必须记录日志并转为业务异常。
7. Controller 直接返回 Entity
   防范：专用 DTO，Long ID 转 String。
8. 日志不足或过多
   防范：关键节点必须记录，敏感字段必须脱敏。
9. 跨模块直接访问
   防范：通过 Service 接口通信。
10. 任务范围不受控
    防范：明确"非目标"，小步修改。

八、多 Agent 协作
8.1 并行分工规则
主 Agent（编排协调）
├── Agent A：独立分支 → 独立任务记录
├── Agent B：独立分支 → 独立任务记录
└── Agent C：独立分支 → 独立任务记录
↓ PR 合并
主 Agent 统一更新公共文档

8.2 代码审查清单
[x] 架构边界：是否跨模块访问
[x] 编码规范：命名、包结构、日志
[x] 测试覆盖：新增功能是否有测试
[x] 数据库：迁移脚本、索引
[x] 安全：密钥、日志脱敏
[x] 文档：是否同步更新
[x] 设计：是否过度设计
九、项目成熟度模型
级别
标志
L0：无规范
Agent 自由发挥，无文档、无记录
L1：有记录
有任务记录和开发日志
L2：有规范
有 AGENTS.md、技术规范、Review 流程
L3：有自动化
CI 自动检查、安全扫描、E2E
L4：自优化
Agent 从历史错误学习，主动改进

十、附录
10.1 每周检查清单
[x] AGENTS.md 是否需要更新
[x] CURRENT_STATE.md 是否反映最新状态
[x] DEVELOPMENT_LOG.md 是否有遗漏
[x] DECISIONS.md 是否有未记录的决策
10.2 术语表
术语
定义
Agent
AI 编程助手（Claude Code、Cursor 等）
Handoff
会话交接
Task Record
任务记录文件
ADR
架构决策记录
E2E
端到端测试
Cold Start
项目初始化流程
